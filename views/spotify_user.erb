<!-- views/spotify_user.erb -->
<h1><%= spotify_user.spotify_user_id %></h1>
<% results.each do |r| %>
  <h2><%= r[:playlist].name %></h2>
  <% if r[:snapshot_created] %>
    <p>🔄 Updated</p>
  <% else %>
    <p>⏭ No changes</p>
  <% end %>
  <h3>➕ Added</h3>
  <ul>
    <% r[:added_tracks].each do |t| %>
      <li><%= t.name %> — <%= t.artists %> - <%= t.duration_ms%> - <%= t.added_at %></li>
    <% end %>
  </ul>
  <h3>➖ Removed</h3>
  <ul>
    <% r[:removed_tracks].each do |t| %>
      <li><%= t.name %> — <%= t.artists %></li>
    <% end %>
  </ul>
<% end %>
